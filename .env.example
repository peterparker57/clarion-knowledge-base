# Clarion Knowledge Base - Environment Configuration Example
# Copy this file to .env and customize as needed

# ============================================================================
# Web App Configuration
# ============================================================================

# Web App Port (default: 8080)
# Change this if port 8080 is already in use on your system
CLARION_WEB_PORT=8080

# ============================================================================
# Security Configuration
# ============================================================================

# Encryption key for API key storage (DO NOT CHANGE after first use!)
# This key is used to encrypt LLM API keys stored by the web app
# WARNING: Changing this will invalidate all previously saved API keys
ENCRYPTION_KEY=yFf7iOiJAhMwF6XO6iRCFkPOLRZnzknWcwdQmSGumpY=

# ============================================================================
# LLM Provider API Keys (Optional)
# ============================================================================

# Pre-configure API keys via environment variables
# If not set here, you can configure them via the web UI

# Deepseek API Key (https://platform.deepseek.com)
# Cost: ~$0.35/1M tokens
# DEEPSEEK_API_KEY=sk-your-key-here

# OpenAI API Key (https://platform.openai.com)
# Cost: $0.38 - $37.50/1M tokens (varies by model)
# OPENAI_API_KEY=sk-your-key-here

# Anthropic API Key (https://console.anthropic.com)
# Cost: $2.40 - $45.00/1M tokens (varies by model)
# ANTHROPIC_API_KEY=sk-your-key-here

# Google Gemini API Key (https://ai.google.dev)
# Cost: $0.19 - $5.63/1M tokens (varies by model)
# GEMINI_API_KEY=your-key-here

# xAI Grok API Key (https://x.ai)
# Cost: $0.35 - $9.00/1M tokens (varies by model)
# GROK_API_KEY=your-key-here

# ============================================================================
# Qdrant Configuration (Advanced - Usually no need to change)
# ============================================================================

# Qdrant connection settings
# For standalone web app: use service name "qdrant"
# For MCP addon mode: use container name "clarion-qdrant"
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# Qdrant collection name
COLLECTION_NAME=clarion_docs

# ============================================================================
# Ollama Configuration (Optional - for local LLMs)
# ============================================================================

# Ollama base URL (if running Ollama locally or in Docker)
# Default: http://localhost:11434
# OLLAMA_URL=http://ollama:11434

# ============================================================================
# Usage Notes
# ============================================================================

# 1. To use this file:
#    cp .env.example .env
#    # Edit .env with your actual values
#
# 2. Load environment variables:
#    docker-compose -f docker-compose-public.yml --env-file .env up -d
#
# 3. Security best practices:
#    - Never commit .env to version control
#    - Rotate API keys periodically
#    - Use separate keys for development and production
#
# 4. Cost management:
#    - Monitor usage via provider dashboards
#    - Set spending limits on provider accounts
#    - Use Deepseek or Gemini Flash for cost-effective queries
#    - Use Ollama (FREE) for unlimited local inference
